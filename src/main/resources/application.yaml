# APP
spring:
  application:
    name: 'portfolio-admin'

# SERVER
server:
  servlet:
    context-path: '/portfolio/admin/v1'

# FIREBASE
# admin-sdk.json should be generated by Firebase and placed in /resources folder
#firebase:
#  app-id: ${APP_ID}
#  service-account-file: admin-sdk.json
#  firebase.database-url=${DB_URL}
#  firebase.storage-url=${STORAGE_URL}
